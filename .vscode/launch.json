{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/zig-out/bin/zserial",
            "args": [],
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "command script import lldb_pretty_printers.py",
                "type category enable zig.lang zig.std zig"
            ]
        }
    ]
}